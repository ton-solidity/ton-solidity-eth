{
  pop(byte(0, shr(0x8, call(0, 0, 0, 0, 0, 0, 0))))
}
// ====
// VMMachineAndVersion: >=constantinople
// ----
// Trace:
//   CALL(0, 0, 0, 0, 0, 0, 0)
// Memory dump:
// Storage dump:
// Transient storage dump:
